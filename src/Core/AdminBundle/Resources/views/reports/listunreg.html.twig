{% extends 'CoreAdminBundle:admin:layout.html.twig' %}
{% block content %}

    {{ include('CoreAdminBundle:reports:style-grid.html.twig') }}
    <div id="title">
        <h1>Usuarios :: Pendientes :: Encontrados: {{ num_res }}</h1>
    </div>
    <div>
    </div>
    {% if grid %}
    <div>
        {{ grid(grid) }}
    </div>
    {% endif %}

{% endblock %}