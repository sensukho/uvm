{% extends 'CoreAdminBundle:admin:layout.html.twig' %}
{% block content %}

        <div id="title">
            <h1>Usuarios :: Agregar</h1>
        </div>

        <div>
            <table>
                <tr align="center">
                    <th colspan="3">
                        {% if msg %}
                        <div id="msg">{{ msg }}</div>
                        {% endif %}
                    </th>
                <tr align="left">
                    <th width="30%"></th>
                    <th width="40%"> {{ form(form) }} </th>
                    <th width="30%"></th>
                </tr>
            </table>
        </div>

{% endblock %}